public class Std {
      int id;
      int age;
      String Name;
      UserAdd user_address= new UserAdd();
      @Override
      public String toString() {
            return "Std [id=" + id + ", age=" + age + ", Name=" + Name + ", user_address=" + user_address + "]";
      }

      
}
